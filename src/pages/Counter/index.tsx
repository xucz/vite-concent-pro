import React from 'react';
import { Counter } from './Counter';
import { CounterTip } from './CounterTip';

export default () => {
  return (
    <div>
      <Counter />
      <CounterTip />
    </div>
  );
};
